# label: Tell the model how to edit the text
form:
  properties:
    instruction:
      $type: textField
      rows: 3
    model:
      $type: comboBoxField
      datasource:
        $type: aiModelsDatasource

actions:
  cancel:
    $type: closeAction